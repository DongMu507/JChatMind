# JChatMind 面试高频问答

这份文档是根据简历中的项目描述重新整理的面试版回答。目标不是堆很多碎问题，而是把面试官可能连续追问的内容合并成一组组综合题，方便面试前快速复习，也方便面试时按一条主线展开。

使用建议：

- 每个问题先记住第一段，这是最适合直接说出口的核心回答。
- 后面的展开内容用于应对追问，不需要一上来全部讲完。
- 涉及效果指标时不要说太满，要讲清楚测试口径和优化思路。
- 涉及 MCP、复杂规划、生产化能力时，要区分“当前代码已经实现的内容”和“后续可优化方案”。

---

## 1. 你这个项目整体是做什么的？为什么说它不是普通 ChatBot？

这个项目是一个基于 Spring AI 的智能 AI Agent 系统，核心目标不是简单接入大模型做一次性问答，而是让大模型具备企业场景里的任务理解、多步决策、工具调用、知识库检索和实时反馈能力。普通 ChatBot 通常是“用户提问，模型回答，一次结束”，而这个项目会根据上下文判断下一步动作：如果知识不足就检索知识库，如果需要外部能力就调用工具，拿到工具结果后再继续推理，直到任务完成。

从后端实现上看，我主要负责的是 Agent 主链路：用户消息进入系统后，后端会持久化消息，创建 Agent 运行实例，恢复会话上下文、Agent 配置、可用工具和知识库，然后进入 Think-Execute 多轮执行循环。过程中产生的 Assistant 消息和 Tool 结果都会保存到数据库，并通过 SSE 实时推送给前端，让用户能看到 Agent 的执行过程和最终结果。

所以我理解这个项目的价值不是“会聊天”，而是把模型、工具、知识库、上下文记忆和工程化基础设施串成一条完整链路。面试时如果需要一句话总结，可以说：这个项目把大模型从单次生成能力，扩展成了一个可执行、可检索、可观测、可持续对话的后端 Agent 系统。

---

## 2. 一次用户提问从前端到最终返回，完整链路是什么？

用户在前端发送消息后，后端首先会把用户消息写入数据库，保证对话历史可追溯。随后系统基于当前会话和 Agent 配置创建运行时 Agent 实例，加载最近上下文、系统提示词、可访问知识库和可用工具，再进入 Think-Execute 循环。每一轮循环中，模型先判断下一步是直接回答还是调用工具；如果调用工具，后端会执行工具并把结果写回上下文，再进入下一轮判断；如果没有工具调用或触发终止条件，Agent 就结束本次任务。

这个过程中，我没有把 Agent 执行当成一个黑盒。Assistant 回复、Tool 调用结果都会被持久化，随后通过 SSE 推给前端。这样前端不只是等一个最终答案，而是可以实时看到模型生成内容、工具执行结果和任务结束状态。这个设计对 Agent 类型产品很重要，因为 Agent 的任务可能持续多轮，如果没有中间状态反馈，用户体验会很差，也不利于定位问题。

如果面试官继续追问代码层面，可以这样说：Agent 主循环在 `JChatMind`，运行时装配在 `JChatMindFactory`，多模型实例通过 `ChatClientRegistry` 获取，RAG 检索由 `RagServiceImpl` 负责，SSE 连接和发送由 `SseServiceImpl` 管理。整体链路围绕“消息入库 -> Agent 实例创建 -> 上下文恢复 -> Think-Execute -> 消息持久化 -> SSE 推送”展开。

---

## 3. Think-Execute 多轮决策机制是怎么设计的？和 ReAct、Planner-Executor 有什么区别？

Think-Execute 是这个项目的 Agent 核心执行模型。Think 阶段让模型基于当前上下文、系统提示词、可用工具和知识库描述，判断下一步动作；Execute 阶段负责真正执行工具调用，并把工具结果写回上下文，让模型在下一轮继续决策。这样可以支持“先查资料，再调用工具，再总结回答”这类多步任务，而不是一次模型调用就结束。

它更接近工程化、简化版的 ReAct，也就是 Reason + Act：模型在推理过程中决定是否行动，工具结果再反过来影响下一轮推理。和 Planner-Executor-Replanner 相比，当前项目没有单独维护显式计划树，也没有复杂的任务 DAG 或 Replanner 模块，更偏“逐步决策型 Agent”。这样设计的原因是项目第一阶段更关注把多轮决策、工具调用、上下文管理和状态反馈稳定跑通，而不是一开始就引入更重的规划框架。

为了防止 Agent 死循环，项目里做了最大步数限制，同时提供终止工具。也就是说，即使模型一直尝试调用工具，后端也会在达到步数上限后强制结束；如果模型判断任务已经完成，也可以通过 terminate 工具主动结束。异常场景下，Agent 会进入错误状态，并通过 SSE 给前端返回错误提示，避免用户界面一直等待。

---

## 4. 工具调用系统是怎么实现的？和 Function Calling、Tool Calling、MCP、Skill 有什么关系？

项目里的工具调用底层依赖 Spring AI 的工具调用能力，本质上属于 Function Calling / Tool Calling 这一类机制：模型返回要调用的工具名和参数，后端根据这个结构化结果执行对应方法。但我没有完全交给框架自动执行，而是手动接管了工具执行流程，这样可以在工具调用前后插入消息持久化、上下文回写、SSE 推送、状态管理和异常处理。

工具体系没有写成 if-else 硬编码，而是通过统一接口和 Spring Bean 注册。运行时由 `JChatMindFactory` 根据 Agent 配置装配工具，工具又分为固定工具和可选工具。固定工具是所有 Agent 都需要的基础能力，比如终止工具；可选工具则根据 Agent 配置开放，比如知识库、数据库、邮件等能力。这样做的好处是新增工具不需要修改 Agent 主流程，不同 Agent 也可以有不同能力边界。

几个概念可以这样区分：Function Calling 更偏模型协议，解决“模型怎么结构化表达调用意图”；Tool Calling 更偏 Agent 能力编排，解决“系统有哪些外部能力可以调用”；Skill 更像任务层抽象，把某类任务的提示词、工具组合和执行策略封装起来；MCP 则是工具或上下文能力的标准化接入协议，解决“外部能力怎么跨系统接进来”。当前代码更偏本地 Tool 体系，如果面试官问 MCP，可以稳妥地说：项目当前先把本地工具调用和 Agent 主链路做扎实，MCP 更适合作为下一阶段标准化工具接入的扩展方向。

---

## 5. 多模型切换是怎么做的？为什么用注册表模式？

项目里没有把某个模型厂商写死在业务流程中，而是把不同模型都注册成 Spring AI 的 `ChatClient Bean`，比如 DeepSeek 和智谱 GLM，再通过 `ChatClientRegistry` 按模型名称获取对应客户端。Agent 运行时只关心当前配置的模型名，不关心底层模型的创建细节。

这样设计的核心是解耦“模型创建”和“模型使用”。如果后续要接入新模型，通常只需要增加对应的 `ChatClient Bean`，再在 Agent 配置中指定模型名称，Agent 主流程不需要改。这比在业务代码里写大量 if-else 更容易扩展，也更符合后端工程里的开闭原则。

如果面试官继续问多模型的生产化问题，可以补充：后续可以在注册表之上加模型路由策略，例如主备模型切换、按任务类型选择模型、按成本选择模型、超时降级和限流保护。当前项目已经具备多模型接入的基础结构，生产环境还可以继续增强为模型网关或模型路由层。

---

## 6. RAG 知识库完整链路是什么？为什么选择 PostgreSQL + pgvector？

RAG 模块做的是完整链路，不只是查向量库。文档上传后，系统先保存文件和文档元数据，然后根据文件类型进行解析：Markdown 可以按标题结构切分，PDF 可以按文本段或页解析，TXT 可以按固定长度或段落切分。切分后的 chunk 会调用 embedding 模型生成向量，再把 chunk 内容、元数据和向量写入 PostgreSQL + pgvector。查询时，系统会把用户问题向量化，通过 pgvector 做相似度检索，返回最相关的 chunk 作为 Agent 回答的外部知识依据。

选择 PostgreSQL + pgvector，主要是因为这个项目除了向量数据，还有 Agent、会话、消息、文档、知识库等结构化业务数据。用 PostgreSQL 可以把结构化数据和向量数据放在一套体系中管理，部署和维护成本更低，也方便后续做权限、事务和数据关联。对于当前项目规模来说，pgvector 已经能支撑基本的向量检索需求，比一开始就引入独立向量数据库更轻量。

如果面试官问为什么不用 Milvus、ES 或 Pinecone，可以这样答：独立向量库在超大规模向量检索、多租户隔离和高性能 ANN 场景下更有优势，但也会带来额外的部署和运维复杂度。这个项目当前目标是把企业知识库问答链路快速稳定落地，所以优先选择 PostgreSQL + pgvector。后续如果数据规模增长或检索性能成为瓶颈，可以再拆出专门的向量检索服务。

---

## 7. RAG 的切块、召回和准确率怎么优化？简历里的 85%+ 怎么回答才稳？

当前项目的 RAG 优化重点是先把解析、分块、向量化、入库和检索主链路打通，并采用相对稳定的切块方式减少噪声。比如 Markdown 按标题结构切分，PDF/TXT 按文本段落或长度切分，查询时控制 topK，只把最相关的内容放入上下文，避免给模型塞入过多无关信息。

如果进一步优化，我会从四个方向做：第一是 chunk overlap，避免答案跨 chunk 时信息断裂；第二是混合检索，把关键词检索和向量检索结合起来，提高专业术语、编号、字段名这类查询的命中率；第三是 rerank，对初召回结果进行二次排序，提高最终上下文质量；第四是建设离线评测集，用人工标注的问题、标准答案和命中文档来评估召回率、准确率和回答质量。

关于简历里的“复杂文档检索准确率 85%+”，面试时不要硬说成严格线上指标。更稳的表达是：项目里确实完成了复杂文档解析、分块、向量检索和召回优化，相比简单关键词匹配有明显提升；85%+ 更适合作为当前测试集或阶段性评估结果，生产化时我会补充标准测试集、人工标注和离线评测流程，让指标有更严格的统计口径。

---

## 8. 长对话上下文压缩怎么设计？为什么用“最近消息窗口 + 历史摘要归档”？

长对话的核心问题是上下文会持续膨胀。如果每次都把所有历史消息原样传给模型，token 成本会越来越高，响应速度会变慢，而且过多无关历史也会干扰模型判断。所以项目里设计了分层记忆方案：最近几轮消息作为短期记忆保留原文，较早的历史对话通过摘要压缩成长期记忆。

实现思路是，当同一个 session 的消息数量超过阈值后，后台异步触发记忆压缩任务，把较早的一批消息交给模型总结成 summary，并保存为 system 类型的摘要消息。之后恢复上下文时，不需要加载完整历史，而是组合“长期摘要 + 最近窗口消息”。这样既保留了用户偏好、历史目标等关键信息，又控制了上下文长度。

这里有一个重要 tradeoff：摘要会损失一部分细节，但可以显著降低 token 成本和上下文噪声；保留最近窗口则是为了让当前对话的细节尽量不丢。为什么异步做摘要？因为摘要不是用户当前请求的强依赖逻辑，如果同步执行会拉长接口耗时。项目里还做了会话级压缩去重和最小压缩间隔，避免高频重复压缩。

---

## 9. 为什么用 SSE 做实时反馈？和 WebSocket 的取舍是什么？

这个项目的实时反馈主要是服务端向前端单向推送：模型生成内容、工具执行结果、Agent 执行状态和结束信号。对于这种场景，SSE 比 WebSocket 更轻量，浏览器原生支持好，协议简单，后端实现成本也更低。所以我选择 SSE 来做 Agent 执行过程的实时可视化。

SSE 的价值在于改善 Agent 产品体验。Agent 任务可能会经历多轮思考、工具调用和结果整合，如果用户只能看到最终答案，中间等待会很不透明。通过 SSE，前端可以实时展示 AI 生成内容、工具返回结果和任务完成状态，用户能感知系统正在执行，而不是页面卡住。

生产化时需要注意连接管理和异常处理。当前实现中使用 `ConcurrentHashMap` 维护 sessionId 到 `SseEmitter` 的映射，并在完成、超时、异常时清理连接。后续可以继续增加心跳、断线重连、消息序号、历史消息补偿和多端连接支持。WebSocket 更适合双向高频通信，比如协同编辑、实时游戏或复杂控制台；这个项目主要是单向推送，所以 SSE 是更合适的阶段性选择。

---

## 10. 可观测性做了哪些？日志、指标、Trace 怎么联动排障？

项目接入了日志、指标和 Trace 三类可观测能力，覆盖 Agent 执行、RAG 检索、Embedding 调用、SSE 连接发送、异步记忆压缩等关键链路。指标可以反映系统整体状态，比如调用次数、耗时、错误数和活跃连接数；Trace 可以串起一次请求经过的关键阶段；日志则用于查看具体上下文和异常原因。

排障时我会按“指标定位阶段，Trace 串联链路，日志确认细节”的方式处理。比如用户反馈回复慢，我会先看指标判断慢在模型调用、RAG 检索、工具执行还是 SSE 推送；然后用 traceId 找到这一条请求经过哪些步骤；最后结合日志看具体报错、工具参数、检索结果或模型异常。这样比只看日志更高效，尤其适合 Agent 这种链路长、阶段多的系统。

如果继续优化，我会补充更明确的业务指标，例如 Agent 单次平均执行轮数、工具调用成功率、RAG 空召回率、Embedding 耗时分位数、SSE 推送失败率、模型调用超时率等。这些指标能帮助判断问题到底是模型效果问题、数据库性能问题，还是工具链路稳定性问题。

---

## 11. 简历里的 2s 首响和 100+ 并发怎么回答才不容易翻车？

这类数字一定要讲清楚测试口径，不能说成绝对线上 SLA。关于 2s 首字响应，我会说这是当前联调和测试环境下的阶段性优化目标，主要通过消息入库后异步触发 Agent、SSE 实时推送、上下文压缩、控制 RAG topK 和埋点定位慢阶段来降低用户感知等待时间。真正线上还要结合模型服务延迟、机器配置、网络环境和压测结果来验证。

关于单节点 100+ 并发，也不要只强调数字本身，而要讲支撑并发的设计。比如主链路尽量异步化，记忆压缩放到后台线程池，SSE 是单向推送，Agent 实例按请求创建，数据库保存会话和消息，指标监控可以观察连接和耗时情况。严格来说，100+ 并发还要看并发类型：是普通接口并发、SSE 长连接并发，还是同时触发模型调用的并发。因为模型供应方限流和响应延迟往往才是真正瓶颈。

稳妥表达是：这些数字代表当前项目在本地或测试环境中的优化目标和阶段性结果，我清楚支撑这些目标用了哪些设计，但如果进入生产，需要用固定机器规格、固定数据规模、固定并发模型和压测报告来严谨验证。

---

## 12. 企业级 AIGC 要解决哪些问题？你的项目目前做到哪一步？

企业级 AIGC 的目标不是做一个能聊天的 demo，而是把大模型变成业务生产力，所以必须解决权限控制、环境隔离、上下文记忆、工具治理、知识库接入、稳定性、可观测性和数据安全等问题。单次调用大模型不能算真正的企业级 AIGC，因为它缺少多步决策、反馈闭环和工程治理。

这个项目目前已经覆盖了几个关键能力：Agent 通过 Think-Execute 做多轮决策；工具系统支持固定工具和可选工具；RAG 支持文档解析、向量化入库和 pgvector 检索；长对话通过摘要和最近窗口控制上下文；SSE 负责实时反馈；Micrometer、Prometheus、Grafana、Trace 提供基础可观测性。

但如果从生产级角度看，还需要继续增强。第一是权限和租户隔离，比如用户只能访问自己有权限的 Agent、知识库和会话；第二是工具沙箱，比如数据库工具只能执行只读白名单 SQL，文件工具只能访问限定目录；第三是密钥治理，不能把数据库密码、邮箱授权码和模型 API Key 放在配置文件明文里；第四是执行隔离，高风险工具可以放到独立 worker 或容器中，通过超时、限流和资源配额控制风险。

---

## 13. 这个项目最大的难点、不足和后续优化方向是什么？

最大的难点不是某一个单点技术，而是把模型调用、工具执行、RAG 检索、上下文记忆、消息持久化、SSE 推送和可观测性串成一条稳定主链路。任何一个环节脱节，项目都可能变成“功能很多但跑不顺”。所以我在设计时重点关注 Agent Loop 的可控性、长链路状态一致性，以及异常情况下前端能否收到明确反馈。

当前项目也有几个不足。第一，规划能力目前更偏多轮决策，不是强 Planner 或复杂工作流编排；第二，RAG 的切块和召回策略还比较基础，需要继续做 overlap、混合检索、rerank 和离线评测；第三，工具安全和权限治理还需要加强，尤其是数据库、文件、邮件这类高风险工具；第四，配置安全需要生产化，敏感信息应该迁移到环境变量、配置中心或密钥管理系统。

如果继续迭代，我会优先做三件事：先补安全治理和权限隔离，保证工具调用可控；再优化 RAG 效果，建立评测集和召回质量指标；最后完善实时链路和异步任务，把 SSE 断线恢复、消息补偿、文档索引状态机、失败重试这些能力补齐。这样项目会从可演示系统进一步接近可生产落地系统。

---

## 14. 如果面试官让你 2 到 3 分钟介绍这个项目，你怎么讲？

我做的这个项目叫 JChatMind，是一个基于 Spring AI 的智能 Agent 系统，技术栈包括 Spring Boot、Spring AI、PostgreSQL、pgvector、MyBatis、SSE、Micrometer、Grafana 和 React。它不是普通聊天机器人，而是围绕大模型实现业务意图理解、多步任务自主决策、工具调用和知识库精准问答。

核心链路是 Think-Execute 多轮决策。用户消息进入后，系统先持久化消息，再创建 Agent 运行实例，恢复会话上下文、Agent 配置、可用工具和知识库。Agent 每一轮先让模型判断下一步动作，如果需要外部能力就调用工具，比如知识库检索、数据库查询或邮件工具；工具结果会重新写回上下文，模型再继续下一轮决策，直到任务完成。为了避免失控，我做了最大步数限制、终止工具和异常兜底。

在 Agent 主链路之外，我还负责了 RAG 知识库模块，支持 Markdown、PDF、TXT 文档解析、分块、Embedding 向量化入库，并基于 PostgreSQL + pgvector 做相似度检索。长对话方面，我设计了“最近消息窗口 + 历史摘要归档”的分层记忆方案，减少上下文冗余和 token 成本。交互体验上，系统通过 SSE 实时推送 AI 生成内容、工具执行结果和 Agent 状态。工程治理上，我接入了日志、指标和 Trace，覆盖 Agent 执行、RAG 检索、异步任务和 SSE 链路，方便定位慢请求和异常。

整体来说，我在这个项目里负责的是把模型、工具、知识、记忆、实时反馈和可观测性串成一条完整的后端 Agent 主链路。这个项目让我对 AI 应用工程化有了更深理解：真正难的不是调用模型，而是让模型在企业业务环境里可控、可扩展、可观测地完成任务。

---

## 面试速记

最应该主动说的关键词：

- Think-Execute 循环
- 多轮决策闭环
- 手动接管 Tool Calling
- Agent 能力边界配置
- RAG 全链路
- PostgreSQL + pgvector
- 最近消息窗口 + 历史摘要归档
- SSE 单向实时推送
- 日志、指标、Trace 联动排障
- 从 demo 到生产的安全治理

最容易被追问的风险点：

- “自主规划”不要说成已经实现复杂 Planner 或工作流引擎，当前更准确是多轮决策型 Agent。
- “MCP”不要硬说成已经完整实现 MCP Server，当前代码更偏本地工具调用体系，MCP 可以作为后续标准化接入方向。
- “85%+ 准确率”要说明测试口径，最好说成阶段性评估结果，生产化需要离线评测集。
- “2s 首响、100+ 并发”不要说成绝对 SLA，要结合环境、机器、模型服务和压测口径。
- 明文配置敏感信息不适合生产，要主动说明会迁移到环境变量、配置中心或密钥管理。

答题万能结构：

先说这个功能解决什么问题，再说项目里怎么实现，然后说为什么这样设计，最后补一句生产化优化方向。这个结构最稳，也最像真实后端开发在讲项目。
